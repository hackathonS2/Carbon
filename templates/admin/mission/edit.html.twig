{% extends 'admin/admin.html.twig' %}

{% block title %}Edit Mission{% endblock %}

{% block body %}
    <h1>Edit Mission</h1>
                                   {% if app.user.roles[0] == 'ROLE_ADMIN' %}

    {{ include('admin/mission/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_mission_index') }}">back to list</a>

    {{ include('admin/mission/_delete_form.html.twig') }}
                                   {% endif %}
                                   {% if app.user.roles[0] == 'ROLE_OPERATIONNEL' %}

    {{ include('operationnel/mission/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('operationnel_mission_index') }}">back to list</a>

    {{ include('admin/mission/_delete_form.html.twig') }}

                                   {% endif %}


{% endblock %}
