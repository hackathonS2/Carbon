<form method="post" action="{{ path('app_admin_test_results_delete', {'id': test_result.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test_result.id) }}">
    <button class="btn">Delete</button>
</form>
