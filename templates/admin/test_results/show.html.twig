{% extends 'base.html.twig' %}

{% block title %}TestResults{% endblock %}

{% block body %}
    <h1>TestResults</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_result.id }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ test_result.result }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ test_result.date ? test_result.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_test_results_index') }}">back to list</a>

    <a href="{{ path('app_admin_test_results_edit', {'id': test_result.id}) }}">edit</a>

    {{ include('admin/test_results/_delete_form.html.twig') }}
{% endblock %}
