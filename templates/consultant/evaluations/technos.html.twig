<div class="mt-4">
		<ul class="flex flex-col px-4">
			{% for techno in technos %}
				<li class="flex flex-col mb-8 shadow-md p-8 mb-8">
					<div class="flex flex-row justify-between w-full">
						<div>
                            <h2 class="font-bold">{{ techno.nom }}</h2>
						<div>
							<a class="text-blue-400" href="{{ path('tests_by_techno', {'id': techno.id}) }}">Passer les tests / formation</a>
						</div>
					</div>

                    <div class="flex flex-row justify-between w-full">
                        <p>
                            {{ techno.description}}
                        </p>
                    </div>

				</li>

			{% endfor %}
		</ul>
</div>