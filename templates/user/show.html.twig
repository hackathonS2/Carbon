{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>IsVerified</th>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>DateDeNaissance</th>
                <td>{{ user.dateDeNaissance ? user.dateDeNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>MailVerifyToken</th>
                <td>{{ user.mailVerifyToken }}</td>
            </tr>
            <tr>
                <th>MailVerifyTokenExp</th>
                <td>{{ user.mailVerifyTokenExp ? user.mailVerifyTokenExp|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ user.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ user.tel }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ user.description }}</td>
            </tr>
            <tr>
                <th>SalaireSouhaite</th>
                <td>{{ user.salaireSouhaite }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ user.updatedAt ? user.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EvalClient</th>
                <td>{{ user.evalClient }}</td>
            </tr>
            <tr>
                <th>LinkLinkedin</th>
                <td>{{ user.linkLinkedin }}</td>
            </tr>
            <tr>
                <th>LinkSlack</th>
                <td>{{ user.linkSlack }}</td>
            </tr>
            <tr>
                <th>EvalClientDev</th>
                <td>{{ user.evalClientDev }}</td>
            </tr>
            <tr>
                <th>ObjectifMensuelCommercial</th>
                <td>{{ user.objectifMensuelCommercial }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}">back to list</a>

    {# <a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a> #}

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
